{
  "name": "akp-11-prompt-browser",
  "version": "1.0.0",
  "private": true,
  "description": "Railway-ready static prompt browser with generated search index.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/generate-index.mjs",
    "check:generated": "npm run build && git diff --exit-code -- index.json catalog.md && git diff --cached --exit-code -- index.json catalog.md",
    "start": "node server.mjs",
    "smoke": "node scripts/check-deployment.mjs",
    "test": "node --test"
  }
}
